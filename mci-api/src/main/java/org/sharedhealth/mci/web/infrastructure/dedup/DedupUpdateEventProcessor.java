package org.sharedhealth.mci.web.infrastructure.dedup;

import java.util.UUID;

public class DedupUpdateEventProcessor implements DedupEventProcessor {

    @Override
    public void process(String healthId, UUID marker) {

    }
}
